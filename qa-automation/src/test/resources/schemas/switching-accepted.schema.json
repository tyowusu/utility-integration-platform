{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SwitchingAccepted",
  "description": "Response to an accepted switching request. Derived from src/main/resources/api/switching-eapi.raml — keep the two in step; a divergence here is a contract break the portals will discover in production.",
  "type": "object",
  "required": ["requestId", "status", "pdr", "effectiveDate", "message"],
  "additionalProperties": false,
  "properties": {
    "requestId": {
      "type": "string",
      "minLength": 1,
      "description": "SII request identifier. The join key for the asynchronous outcome."
    },
    "status": {
      "type": "string",
      "enum": ["SUBMITTED", "ALREADY_SUBMITTED"]
    },
    "pdr": {
      "type": "string",
      "pattern": "^[0-9]{14}$"
    },
    "effectiveDate": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-01$",
      "description": "Always the first of a month. The pattern enforces the regulatory constraint at the contract level, so a rule-module regression cannot pass schema validation."
    },
    "outcomeExpectedBy": {
      "type": ["string", "null"],
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    },
    "message": {
      "type": "string",
      "minLength": 1
    }
  }
}
