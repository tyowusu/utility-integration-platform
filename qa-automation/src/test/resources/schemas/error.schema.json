{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Error",
  "description": "Uniform error envelope. Every channel parses this one shape, so any deviation breaks all three consumers at once.",
  "type": "object",
  "required": ["error"],
  "additionalProperties": false,
  "properties": {
    "error": {
      "type": "object",
      "required": ["code", "message", "correlationId", "timestamp"],
      "additionalProperties": false,
      "properties": {
        "code": {
          "type": "string",
          "minLength": 1
        },
        "message": {
          "type": "string",
          "minLength": 1
        },
        "correlationId": {
          "type": "string",
          "minLength": 1,
          "description": "Join key to the audit record. A regulator or ombudsman enquiry starts from a customer holding this value; without it the trail begins nowhere."
        },
        "timestamp": {
          "type": "string"
        }
      }
    }
  }
}
